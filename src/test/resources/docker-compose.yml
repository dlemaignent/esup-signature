version: '3.1'

services:
  db:
    image: postgres:latest
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: esupsignature
      POSTGRES_PASSWORD: esup
      POSTGRES_DB: esupsignature

  ghostscript:
    build:
      context: .
      dockerfile: Dockerfile.gs
    volumes:
      - /chemin/vers/vos/fichiers:/data  # Montez vos fichiers si nécessaire
